# pnpm 11 corre una verificación de dependencias antes de cada script que puede
# fallar por scripts de build ignorados (p. ej. @scarf/scarf, telemetría). La
# desactivamos para que `pnpm db:deploy` / `pnpm build` corran sin ese tropiezo.
verify-deps-before-run=false
